// src/lib/live-room.ts
export const DEFAULT_LIVE_ROOM = 'KyomeikaiLiveRoom';

// 必要に応じて日付ベースなどの生成もここで
// export const liveRoomForToday = () => `kyomeikai-live-${new Date().toISOString().slice(0,10)}`;
